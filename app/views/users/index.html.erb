<h1>All users</h1>

<%= form_with url: users_path, method: :get do |f| %>
  <%= f.search_field :q, value: @query, placeholder: "ユーザーを検索" %>
  <%= f.submit "検索" %>
<% end %>

<%= will_paginate %>

<ul>
  <%= render @users %>
</ul>

<%= will_paginate %>
